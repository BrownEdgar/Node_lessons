const counter = function(arr){
    return `ajstex ka ${arr.length} elements`
}

let sum = (a, b) =>{
    return `2 tveri gumary havasar e ${a+b}`
}
let pi = Math.PI.toFixed(4);

module.exports = {
    counter,
    sum,
    pi,
}
