<h1>Example App: {{name}}</h1>
{{#if bool}}
<p>My home page is worcking!!!</p>

{{else}}
<p>My home page is not!!! worcking!!!</p>

{{/if }}

{{#each users}}
	{{#if this.regeon }}
	<div>
		<p>info: {{this.name}}</p>
	</div>
	{{/if}}


{{/each}}